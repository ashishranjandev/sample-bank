spring:
  application:
    name: sample-bank
    datasource:
      url: jdbc:h2:~/test;DB_CLOSE_DELAY=-1;
      driverClass: org.h2.driver
      username: sa
      password:
      dialect: org.hibernate.dialect.H2Dialect
  jpa:
    hibernate:
      ddl-auto: none
